# BASE TEMPLATE — no feeds by design.
#
# Every client researches its own subject, so real feeds belong in
# clients/<name>/sources.yaml, never here. A feed left in this file would be
# pulled into every client's research.
#
# Copy clients/example/sources.yaml as a starting point.

feeds: []

keywords: {}
