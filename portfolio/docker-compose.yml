services:
  axiomgate-labs:
    build: .
    image: axiomgate-labs-portfolio:latest
    container_name: axiomgate-labs-portfolio
    restart: unless-stopped
    ports:
      # host:container — change the host port (7000) if it's already taken on your Unraid box
      - "7000:80"
